# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
.pytest_cache/
.coverage
htmlcov/
.venv/
venv/
env/

# Node / frontend
node_modules/
frontend/dist/
frontend/test-results/
frontend/playwright-report/
*.local

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Local / temp
_src_api/
_src_designer/
*.log

# Pulled from HA via MCP (may contain SSIDs / device-specific text); keep local only
tests/fixtures/heating-controller.yaml

# Parity on macOS: HA URL + device_id (see scripts/parity-local.env.example)
scripts/.parity-local.env

# Secrets (do not commit)
.env
.env.*
!.env.example
.github-token
# Mac SDL agent token if stored inside the repo (prefer ~/.esptoolkit_mac_sim_token instead)
tools/mac_esphome_sim_agent/.mac_sim_token
tools/mac_esphome_sim_agent/mac_sim_token.txt
