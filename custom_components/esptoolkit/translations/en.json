{
  "config": {
    "step": {
      "user": {
        "title": "EspToolkit"
      }
    },
    "abort": {
      "configure_via_addon": "Configure the EspToolkit add-on (Settings → Add-ons → EspToolkit → Configuration): set the API token and ensure the add-on is running. The integration will be set up automatically.",
      "invalid_config": "Invalid configuration (base_url and token required).",
      "already_configured": "Integration is already configured."
    }
  }
}
